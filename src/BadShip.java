import processing.core.PImage;

import java.util.List;

public class BadShip extends Entity{

    public BadShip(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
