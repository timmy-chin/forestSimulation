import processing.core.PImage;

import java.util.List;

public class GoodShip extends Entity{

    public GoodShip(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
